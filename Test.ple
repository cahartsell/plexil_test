Command debugMsg( String msg );
Command driveToNextWaypoint();

Real Lookup at_waypoint

Main: Concurrence
{
  Boolean drive_done = false;

  DriveToWaypoint:
  {
    RepeatCondition Lookup(at_waypoint) = true;
    ExitCondition drive_done = true;
    driveToNextWaypoint();
  }

  Bumper Triggered
  Tilt sensor Triggered
  Drive to charging Dock
}